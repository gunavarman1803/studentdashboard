'use strict';

var tslib_es6 = require('../../node_modules/tslib/tslib.es6.js');
var React = require('react');
var PropTypes = require('prop-types');
var index = require('../../node_modules/classnames/index.js');
var CButton = require('../button/CButton.js');
var utils = require('./utils.js');
var useForkedRef = require('../../hooks/useForkedRef.js');
require('@popperjs/core');
var useStateWithCallback = require('../../hooks/useStateWithCallback.js');

var Calendar = function (props) {
    var calendarDate = props.calendarDate, dayFormat = props.dayFormat, disabledDates = props.disabledDates, endDate = props.endDate, firstDayOfWeek = props.firstDayOfWeek, hoverDate = props.hoverDate, locale = props.locale, maxDate = props.maxDate, minDate = props.minDate, onCalendarClick = props.onCalendarClick, onCalendarKeyDown = props.onCalendarKeyDown, onCalendarMouseEnter = props.onCalendarMouseEnter, onCalendarMouseLeave = props.onCalendarMouseLeave, selectAdjacementDays = props.selectAdjacementDays, selectEndDate = props.selectEndDate, selectionType = props.selectionType, showWeekNumber = props.showWeekNumber, showAdjacementDays = props.showAdjacementDays, startDate = props.startDate, view = props.view, weekdayFormat = props.weekdayFormat, weekNumbersLabel = props.weekNumbersLabel;
    var _a = React.useState([]), listOfMonths = _a[0], setListOfMonths = _a[1];
    React.useEffect(function () {
        setListOfMonths(utils.createGroupsInArray(utils.getMonthsNames(locale), 4));
    }, []);
    var monthDetails = utils.getMonthDetails(calendarDate.getFullYear(), calendarDate.getMonth(), firstDayOfWeek);
    var listOfYears = utils.createGroupsInArray(utils.getYears(calendarDate.getFullYear()), 4);
    var weekDays = monthDetails[0].days;
    return (React.createElement("table", null,
        view === 'days' && (React.createElement("thead", null,
            React.createElement("tr", null,
                showWeekNumber && (React.createElement("th", { className: "calendar-cell" },
                    React.createElement("div", { className: "calendar-header-cell-inner" }, weekNumbersLabel))),
                weekDays.map(function (_a, idx) {
                    var date = _a.date;
                    return (React.createElement("th", { key: idx, className: "calendar-cell" },
                        React.createElement("div", { className: "calendar-header-cell-inner" }, typeof weekdayFormat === 'function'
                            ? weekdayFormat(date)
                            : typeof weekdayFormat === 'string'
                                ? date.toLocaleDateString(locale, { weekday: weekdayFormat })
                                : date
                                    .toLocaleDateString(locale, { weekday: 'long' })
                                    .slice(0, weekdayFormat))));
                })))),
        React.createElement("tbody", null,
            view === 'days' &&
                monthDetails.map(function (week, index$1) {
                    var date = utils.convertToDateObject(week.weekNumber === 0
                        ? "".concat(calendarDate.getFullYear(), "W53")
                        : "".concat(calendarDate.getFullYear(), "W").concat(week.weekNumber), selectionType);
                    var isDisabled = utils.isDateDisabled(date, minDate, maxDate, disabledDates);
                    var current = week.days.some(function (day) { return day.month === 'current'; });
                    return (React.createElement("tr", tslib_es6.__assign({ className: index.default('calendar-row', {
                            disabled: isDisabled,
                            range: selectionType === 'week' && utils.isDateInRange(date, startDate, endDate),
                            'range-hover': selectionType === 'week' && hoverDate && selectEndDate
                                ? utils.isDateInRange(date, startDate, hoverDate)
                                : utils.isDateInRange(date, hoverDate, endDate),
                            selected: utils.isDateSelected(date, startDate, endDate),
                        }), key: index$1, tabIndex: selectionType === 'week' && current && !isDisabled ? 0 : -1 }, (selectionType === 'week' &&
                        !isDisabled && {
                        onBlur: function () { return onCalendarMouseLeave(); },
                        onClick: function () { return onCalendarClick(date); },
                        onFocus: function () { return onCalendarMouseEnter(date); },
                        onKeyDown: function (event) { return onCalendarKeyDown(event, date); },
                        onMouseEnter: function () { return onCalendarMouseEnter(date); },
                        onMouseLeave: function () { return onCalendarMouseLeave(); },
                    })),
                        showWeekNumber && (React.createElement("th", { className: "calendar-cell-week-number" }, week.weekNumber === 0 ? 53 : week.weekNumber)),
                        week.days.map(function (_a, idx) {
                            var _b;
                            var date = _a.date, month = _a.month;
                            var isDisabled = utils.isDateDisabled(date, minDate, maxDate, disabledDates);
                            return month === 'current' || showAdjacementDays ? (React.createElement("td", tslib_es6.__assign({ className: index.default('calendar-cell', tslib_es6.__assign(tslib_es6.__assign({}, (selectionType === 'day' && {
                                    clickable: month !== 'current' && selectAdjacementDays,
                                    disabled: isDisabled,
                                    'range-hover': month === 'current' &&
                                        (hoverDate && selectEndDate
                                            ? utils.isDateInRange(date, startDate, hoverDate)
                                            : utils.isDateInRange(date, hoverDate, endDate)),
                                    range: month === 'current' && utils.isDateInRange(date, startDate, endDate),
                                    selected: utils.isDateSelected(date, startDate, endDate),
                                })), (_b = {}, _b[month] = true, _b.today = month === 'current' && utils.isToday(date), _b))), key: idx, tabIndex: selectionType === 'day' &&
                                    (month === 'current' || selectAdjacementDays) &&
                                    !isDisabled
                                    ? 0
                                    : -1, title: date.toLocaleDateString(locale) }, (selectionType === 'day' &&
                                (month === 'current' || selectAdjacementDays) && {
                                onBlur: function () { return onCalendarMouseLeave(); },
                                onClick: function () { return onCalendarClick(date); },
                                onFocus: function () { return onCalendarMouseEnter(date); },
                                onKeyDown: function (event) { return onCalendarKeyDown(event, date); },
                                onMouseEnter: function () { return onCalendarMouseEnter(date); },
                                onMouseLeave: function () { return onCalendarMouseLeave(); },
                            }), (month !== 'current' &&
                                !selectAdjacementDays && {
                                onMouseEnter: function () { return onCalendarMouseLeave(); },
                            })),
                                React.createElement("div", { className: "calendar-cell-inner" }, typeof dayFormat === 'function'
                                    ? dayFormat(date)
                                    : date.toLocaleDateString(locale, { day: dayFormat })))) : (React.createElement("td", { key: idx }));
                        })));
                }),
            view === 'months' &&
                listOfMonths.map(function (row, index$1) {
                    return (React.createElement("tr", { key: index$1 }, row.map(function (month, idx) {
                        var monthNumber = index$1 * 3 + idx;
                        var date = new Date(Date.UTC(calendarDate.getFullYear(), monthNumber, 1));
                        var isDisabled = utils.isDateDisabled(date, minDate, maxDate, disabledDates);
                        return (React.createElement("td", tslib_es6.__assign({ className: index.default('calendar-cell', {
                                disabled: isDisabled,
                                selected: utils.isDateSelected(date, startDate, endDate),
                                'range-hover': selectionType === 'month' &&
                                    (hoverDate && selectEndDate
                                        ? utils.isDateInRange(date, startDate, hoverDate)
                                        : utils.isDateInRange(date, hoverDate, endDate)),
                                range: utils.isDateInRange(date, startDate, endDate),
                            }), key: idx, tabIndex: isDisabled ? -1 : 0 }, (!isDisabled && {
                            onBlur: function () { return onCalendarMouseLeave(); },
                            onClick: function () { return onCalendarClick(date); },
                            onFocus: function () { return onCalendarMouseEnter(date); },
                            onKeyDown: function (event) { return onCalendarKeyDown(event, date); },
                            onMouseEnter: function () { return onCalendarMouseEnter(date); },
                            onMouseLeave: function () { return onCalendarMouseLeave(); },
                        })),
                            React.createElement("div", { className: "calendar-cell-inner" }, month)));
                    })));
                }),
            view === 'years' &&
                listOfYears.map(function (row, index$1) {
                    return (React.createElement("tr", { key: index$1 }, row.map(function (year, idx) {
                        var date = new Date(Date.UTC(year, 0, 1));
                        var isDisabled = utils.isDateDisabled(date, minDate, maxDate, disabledDates);
                        return (React.createElement("td", tslib_es6.__assign({ className: index.default('calendar-cell year', {
                                disabled: isDisabled,
                                selected: utils.isDateSelected(date, startDate, endDate),
                                'range-hover': selectionType === 'year' &&
                                    (hoverDate && selectEndDate
                                        ? utils.isDateInRange(date, startDate, hoverDate)
                                        : utils.isDateInRange(date, hoverDate, endDate)),
                                range: utils.isDateInRange(date, startDate, endDate),
                            }), key: idx, tabIndex: isDisabled ? -1 : 0 }, (!isDisabled && {
                            onBlur: function () { return onCalendarMouseLeave(); },
                            onClick: function () { return onCalendarClick(date); },
                            onFocus: function () { return onCalendarMouseEnter(date); },
                            onKeyDown: function (event) { return onCalendarKeyDown(event, date); },
                            onMouseEnter: function () { return onCalendarMouseEnter(date); },
                            onMouseLeave: function () { return onCalendarMouseLeave(); },
                        })),
                            React.createElement("div", { className: "calendar-cell-inner" }, new Date(year, 0, 1).toLocaleDateString(locale, { year: 'numeric' }))));
                    })));
                }))));
};
var Navigation = function (props) {
    var calendarDate = props.calendarDate, locale = props.locale, navigation = props.navigation, navNextDoubleIcon = props.navNextDoubleIcon, navNextIcon = props.navNextIcon, navPrevDoubleIcon = props.navPrevDoubleIcon, navPrevIcon = props.navPrevIcon, navYearFirst = props.navYearFirst, onMonthClick = props.onMonthClick, onNavigationClick = props.onNavigationClick, onYearClick = props.onYearClick, view = props.view;
    return (React.createElement("div", { className: "calendar-nav" },
        navigation && (React.createElement("div", { className: "calendar-nav-prev" },
            React.createElement(CButton.CButton, { color: "transparent", size: "sm", onClick: function () { return onNavigationClick('prev', true); } }, navPrevDoubleIcon !== null && navPrevDoubleIcon !== void 0 ? navPrevDoubleIcon : (React.createElement("span", { className: "calendar-nav-icon calendar-nav-icon-double-prev" }))),
            view === 'days' && (React.createElement(CButton.CButton, { color: "transparent", size: "sm", onClick: function () { return onNavigationClick('prev'); } }, navPrevIcon !== null && navPrevIcon !== void 0 ? navPrevIcon : React.createElement("span", { className: "calendar-nav-icon calendar-nav-icon-prev" }))))),
        React.createElement("div", tslib_es6.__assign({ className: "calendar-nav-date" }, (navYearFirst && { style: { display: 'flex', justifyContent: 'center' } })),
            view === 'days' && (React.createElement(CButton.CButton, { color: "transparent", size: "sm", onClick: function () { return navigation && onMonthClick(); } }, calendarDate && calendarDate.toLocaleDateString(locale, { month: 'long' }))),
            React.createElement(CButton.CButton, tslib_es6.__assign({ color: "transparent", size: "sm", onClick: function () { return navigation && onYearClick(); } }, (navYearFirst && { style: { order: '-1' } })), calendarDate && calendarDate.toLocaleDateString(locale, { year: 'numeric' }))),
        navigation && (React.createElement("div", { className: "calendar-nav-next" },
            view === 'days' && (React.createElement(CButton.CButton, { color: "transparent", size: "sm", onClick: function () { return onNavigationClick('next'); } }, navNextIcon !== null && navNextIcon !== void 0 ? navNextIcon : React.createElement("span", { className: "calendar-nav-icon calendar-nav-icon-next" }))),
            React.createElement(CButton.CButton, { color: "transparent", size: "sm", onClick: function () { return onNavigationClick('next', true); } }, navNextDoubleIcon !== null && navNextDoubleIcon !== void 0 ? navNextDoubleIcon : (React.createElement("span", { className: "calendar-nav-icon calendar-nav-icon-double-next" })))))));
};
var CCalendar = React.forwardRef(function (_a, ref) {
    var _b;
    var startDate = _a.startDate, endDate = _a.endDate, _c = _a.calendarDate, calendarDate = _c === void 0 ? startDate || endDate || null : _c, _d = _a.calendars, calendars = _d === void 0 ? 1 : _d, className = _a.className, _e = _a.dayFormat, dayFormat = _e === void 0 ? 'numeric' : _e, disabledDates = _a.disabledDates, _f = _a.firstDayOfWeek, firstDayOfWeek = _f === void 0 ? 1 : _f, _g = _a.locale, locale = _g === void 0 ? 'default' : _g, maxDate = _a.maxDate, minDate = _a.minDate, _h = _a.navigation, navigation = _h === void 0 ? true : _h, navNextIcon = _a.navNextIcon, navNextDoubleIcon = _a.navNextDoubleIcon, navPrevIcon = _a.navPrevIcon, navPrevDoubleIcon = _a.navPrevDoubleIcon, navYearFirst = _a.navYearFirst, range = _a.range, _j = _a.selectAdjacementDays, selectAdjacementDays = _j === void 0 ? false : _j, selectEndDate = _a.selectEndDate, _k = _a.selectionType, selectionType = _k === void 0 ? 'day' : _k, _l = _a.showAdjacementDays, showAdjacementDays = _l === void 0 ? true : _l, _m = _a.showWeekNumber, showWeekNumber = _m === void 0 ? false : _m, _o = _a.weekdayFormat, weekdayFormat = _o === void 0 ? 2 : _o, weekNumbersLabel = _a.weekNumbersLabel, onCalendarDateChange = _a.onCalendarDateChange, onDateHover = _a.onDateHover, onEndDateChange = _a.onEndDateChange, onStartDateChange = _a.onStartDateChange, onSelectEndChange = _a.onSelectEndChange, onViewChanged = _a.onViewChanged;
    var calendarRef = React.useRef(null);
    var forkedRef = useForkedRef.useForkedRef(ref, calendarRef);
    var isInitialMount = React.useRef(true);
    var _p = React.useState(null), _calendarDate = _p[0], setCalendarDate = _p[1];
    React.useEffect(function () {
        if (selectionType === 'day' || selectionType === 'week') {
            setView('days');
            return;
        }
        if (selectionType === 'month') {
            setView('months');
            return;
        }
        if (selectionType === 'year') {
            setView('years');
            return;
        }
    }, [selectionType]);
    React.useEffect(function () {
        if (calendarDate === null) {
            setCalendarDate(new Date());
            return;
        }
        if (calendarDate) {
            var date = utils.convertToDateObject(calendarDate, selectionType);
            !utils.isSameDateAs(_calendarDate, date) && setCalendarDate(date);
        }
    }, [calendarDate]);
    var _q = useStateWithCallback.useStateWithCallback(startDate ? utils.convertToDateObject(startDate, selectionType) : null, function () {
        return onStartDateChange && onStartDateChange(utils.getDateBySelectionType(_startDate, selectionType));
    }, !isInitialMount.current), _startDate = _q[0], setStartDate = _q[1];
    React.useEffect(function () {
        var date = startDate ? utils.convertToDateObject(startDate, selectionType) : null;
        if (!utils.isSameDateAs(date, _startDate)) {
            setStartDate(date);
        }
    }, [startDate]);
    var _r = useStateWithCallback.useStateWithCallback(endDate ? utils.convertToDateObject(endDate, selectionType) : null, function () { return onEndDateChange && onEndDateChange(utils.getDateBySelectionType(_endDate, selectionType)); }, !isInitialMount.current), _endDate = _r[0], setEndDate = _r[1];
    React.useEffect(function () {
        var date = endDate ? utils.convertToDateObject(endDate, selectionType) : null;
        if (!utils.isSameDateAs(date, _endDate)) {
            setEndDate(date);
        }
    }, [endDate]);
    var _s = React.useState(null), _hoverDate = _s[0], setHoverDate = _s[1];
    var _t = React.useState(maxDate ? utils.convertToDateObject(maxDate, selectionType) : null), _maxDate = _t[0], setMaxDate = _t[1];
    React.useEffect(function () {
        maxDate && setMaxDate(utils.convertToDateObject(maxDate, selectionType));
    }, [maxDate]);
    var _u = React.useState(minDate ? utils.convertToDateObject(minDate, selectionType) : null), _minDate = _u[0], setMinDate = _u[1];
    React.useEffect(function () {
        minDate && setMinDate(utils.convertToDateObject(minDate, selectionType));
    }, [minDate]);
    var _v = useStateWithCallback.useStateWithCallback(selectEndDate, onSelectEndChange), _selectEndDate = _v[0], setSelectEndDate = _v[1];
    React.useEffect(function () {
        setSelectEndDate(selectEndDate);
    }, [selectEndDate]);
    React.useEffect(function () {
        !isInitialMount.current &&
            typeof _selectEndDate === 'boolean' &&
            onSelectEndChange &&
            onSelectEndChange(_selectEndDate);
    }, [_selectEndDate]);
    var _w = useStateWithCallback.useStateWithCallback('days', onViewChanged), view = _w[0], setView = _w[1];
    React.useEffect(function () {
        isInitialMount.current = false;
    }, []);
    var setCalendarPage = function (years, months, setMonth) {
        if (months === void 0) { months = 0; }
        if (_calendarDate === null) {
            return;
        }
        var year = _calendarDate.getFullYear();
        var month = _calendarDate.getMonth();
        var d = new Date(year, month, 1);
        years && d.setFullYear(d.getFullYear() + years);
        months && d.setMonth(d.getMonth() + months);
        setCalendarDate(d);
        onCalendarDateChange && onCalendarDateChange(d);
    };
    var handleCalendarClick = function (date, index) {
        if (utils.isDateDisabled(date, _minDate, _maxDate, disabledDates)) {
            return;
        }
        var _date = new Date(date);
        if (view === 'days') {
            setCalendarDate(index ? new Date(_date.setMonth(_date.getMonth() - index)) : _date);
        }
        if (view === 'months' && selectionType !== 'month') {
            setCalendarDate(index ? new Date(_date.setMonth(_date.getMonth() - index)) : _date);
            setView('days');
            return;
        }
        if (view === 'years' && selectionType !== 'year') {
            setCalendarDate(index ? new Date(_date.setFullYear(_date.getFullYear() - index)) : _date);
            setView('months');
            return;
        }
        if (range) {
            if (_selectEndDate) {
                setSelectEndDate(false);
                if (_startDate && _startDate > date) {
                    setStartDate(null);
                    setEndDate(null);
                    return;
                }
                if (utils.isDisableDateInRange(_startDate, date, disabledDates)) {
                    setStartDate(null);
                    setEndDate(null);
                    return;
                }
                setEndDate(date);
                return;
            }
            if (_endDate && _endDate < date) {
                setStartDate(null);
                setEndDate(null);
                return;
            }
            if (utils.isDisableDateInRange(date, _endDate, disabledDates)) {
                setStartDate(null);
                setEndDate(null);
                return;
            }
            setSelectEndDate(true);
            setStartDate(date);
            return;
        }
        setStartDate(date);
    };
    var handleCalendarKeyDown = function (event, date, index) {
        if (event.code === 'Space' || event.key === 'Enter') {
            event.preventDefault();
            handleCalendarClick(date, index);
        }
        if (event.key === 'ArrowRight' ||
            event.key === 'ArrowLeft' ||
            event.key === 'ArrowUp' ||
            event.key === 'ArrowDown') {
            event.preventDefault();
            if (maxDate &&
                date >= utils.convertToDateObject(maxDate, selectionType) &&
                (event.key === 'ArrowRight' || event.key === 'ArrowDown')) {
                return;
            }
            if (minDate &&
                date <= utils.convertToDateObject(minDate, selectionType) &&
                (event.key === 'ArrowLeft' || event.key === 'ArrowUp')) {
                return;
            }
            var element_1 = event.target;
            if (selectionType === 'week' && element_1.tabIndex === -1) {
                element_1 = element_1.closest('tr[tabindex="0"]');
            }
            var list_1 = [].concat.apply([], Element.prototype.querySelectorAll.call(calendarRef.current, selectionType === 'week' ? 'tr[tabindex="0"]' : 'td[tabindex="0"]'));
            var index_1 = list_1.indexOf(element_1);
            var first = index_1 === 0;
            var last = index_1 === list_1.length - 1;
            var toBoundary = {
                start: index_1,
                end: list_1.length - (index_1 + 1),
            };
            var gap_1 = {
                ArrowRight: 1,
                ArrowLeft: -1,
                ArrowUp: selectionType === 'week' && view === 'days' ? -1 : view === 'days' ? -7 : -3,
                ArrowDown: selectionType === 'week' && view === 'days' ? 1 : view === 'days' ? 7 : 3,
            };
            if ((event.key === 'ArrowRight' && last) ||
                (event.key === 'ArrowDown' && toBoundary['end'] < gap_1['ArrowDown']) ||
                (event.key === 'ArrowLeft' && first) ||
                (event.key === 'ArrowUp' && toBoundary['start'] < Math.abs(gap_1['ArrowUp']))) {
                if (view === 'days') {
                    setCalendarPage(0, event.key === 'ArrowRight' || event.key === 'ArrowDown' ? 1 : -1);
                }
                if (view === 'months') {
                    setCalendarPage(event.key === 'ArrowRight' || event.key === 'ArrowDown' ? 1 : -1);
                }
                if (view === 'years') {
                    setCalendarPage(event.key === 'ArrowRight' || event.key === 'ArrowDown' ? 10 : -10);
                }
                setTimeout(function () {
                    var _a;
                    var _list = [].concat.apply([], Element.prototype.querySelectorAll.call((_a = element_1.parentNode) === null || _a === void 0 ? void 0 : _a.parentNode, 'td[tabindex="0"], tr[tabindex="0"]'));
                    if (_list.length > 0 && event.key === 'ArrowRight') {
                        _list[0].focus();
                    }
                    if (_list.length > 0 && event.key === 'ArrowLeft') {
                        _list[_list.length - 1].focus();
                    }
                    if (_list.length > 0 && event.key === 'ArrowDown') {
                        _list[gap_1['ArrowDown'] - (list_1.length - index_1)].focus();
                    }
                    if (_list.length > 0 && event.key === 'ArrowUp') {
                        _list[_list.length - (Math.abs(gap_1['ArrowUp']) + 1 - (index_1 + 1))].focus();
                    }
                }, 1);
                return;
            }
            if (list_1[index_1 + gap_1[event.key]].tabIndex === 0) {
                list_1[index_1 + gap_1[event.key]].focus();
                return;
            }
            for (var i = index_1; i < list_1.length; event.key === 'ArrowRight' || event.key === 'ArrowDown' ? i++ : i--) {
                if (list_1[i + gap_1[event.key]].tabIndex === 0) {
                    list_1[i + gap_1[event.key]].focus();
                    break;
                }
            }
        }
    };
    var handleCalendarMouseEnter = function (date) {
        if (utils.isDateDisabled(date, _minDate, _maxDate, disabledDates)) {
            return;
        }
        setHoverDate(date);
        date && onDateHover && onDateHover(utils.getDateBySelectionType(date, selectionType));
    };
    var handleCalendarMouseLeave = function () {
        setHoverDate(null);
        onDateHover && onDateHover(null);
    };
    var handleNavigationOnClick = function (direction, double) {
        if (double === void 0) { double = false; }
        if (direction === 'prev') {
            if (double) {
                setCalendarPage(view === 'years' ? -10 : -1);
                return;
            }
            if (view !== 'days') {
                setCalendarPage(-1);
                return;
            }
            setCalendarPage(0, -1);
            return;
        }
        if (direction === 'next') {
            if (double) {
                setCalendarPage(view === 'years' ? 10 : 1);
                return;
            }
            if (view !== 'days') {
                setCalendarPage(1);
                return;
            }
            setCalendarPage(0, 1);
            return;
        }
    };
    return (React.createElement("div", { className: index.default('calendars', (_b = {},
            _b["select-".concat(selectionType)] = selectionType && view === 'days',
            _b['show-week-numbers'] = showWeekNumber,
            _b), className), ref: forkedRef }, _calendarDate &&
        Array.from({ length: calendars }, function (_, index$1) {
            var calendarDate = utils.getCalendarDate(_calendarDate, index$1, view);
            return (React.createElement("div", { className: index.default('calendar', view), key: index$1 },
                React.createElement(Navigation, { calendarDate: calendarDate, locale: locale, navigation: navigation, navNextDoubleIcon: navNextDoubleIcon, navNextIcon: navNextIcon, navPrevDoubleIcon: navPrevDoubleIcon, navPrevIcon: navPrevIcon, navYearFirst: navYearFirst, onMonthClick: function () { return setView('months'); }, onNavigationClick: handleNavigationOnClick, onYearClick: function () { return setView('years'); }, view: view }),
                React.createElement(Calendar, { calendarDate: calendarDate, dayFormat: dayFormat, disabledDates: disabledDates, endDate: _endDate, firstDayOfWeek: firstDayOfWeek, hoverDate: _hoverDate, locale: locale, maxDate: _maxDate, minDate: _minDate, onCalendarClick: function (date) { return handleCalendarClick(date, index$1); }, onCalendarKeyDown: function (event, date) { return handleCalendarKeyDown(event, date, index$1); }, onCalendarMouseEnter: handleCalendarMouseEnter, onCalendarMouseLeave: handleCalendarMouseLeave, order: index$1, selectAdjacementDays: selectAdjacementDays, selectEndDate: _selectEndDate, selectionType: selectionType, showAdjacementDays: showAdjacementDays, showWeekNumber: showWeekNumber, startDate: _startDate, view: view, weekdayFormat: weekdayFormat, weekNumbersLabel: weekNumbersLabel })));
        })));
});
CCalendar.propTypes = {
    className: PropTypes.string,
    calendarDate: PropTypes.oneOfType([PropTypes.instanceOf(Date), PropTypes.string]),
    calendars: PropTypes.number,
    dayFormat: PropTypes.oneOfType([
        PropTypes.func,
        PropTypes.oneOf(['2-digit', 'numeric']),
    ]),
    disabledDates: PropTypes.array,
    endDate: PropTypes.oneOfType([PropTypes.instanceOf(Date), PropTypes.string]),
    firstDayOfWeek: PropTypes.number,
    locale: PropTypes.string,
    maxDate: PropTypes.oneOfType([PropTypes.instanceOf(Date), PropTypes.string]),
    minDate: PropTypes.oneOfType([PropTypes.instanceOf(Date), PropTypes.string]),
    navigation: PropTypes.bool,
    navNextIcon: PropTypes.node,
    navNextDoubleIcon: PropTypes.node,
    navPrevIcon: PropTypes.node,
    navPrevDoubleIcon: PropTypes.node,
    navYearFirst: PropTypes.bool,
    range: PropTypes.bool,
    selectAdjacementDays: PropTypes.bool,
    selectEndDate: PropTypes.bool,
    selectionType: PropTypes.oneOf(['day', 'week', 'month', 'year']),
    showAdjacementDays: PropTypes.bool,
    showWeekNumber: PropTypes.bool,
    startDate: PropTypes.oneOfType([PropTypes.instanceOf(Date), PropTypes.string]),
    weekdayFormat: PropTypes.oneOfType([
        PropTypes.func,
        PropTypes.number,
        PropTypes.oneOf(['long', 'narrow', 'short']),
    ]),
    weekNumbersLabel: PropTypes.string,
    onDateHover: PropTypes.func,
    onCalendarDateChange: PropTypes.func,
    onEndDateChange: PropTypes.func,
    onSelectEndChange: PropTypes.func,
    onStartDateChange: PropTypes.func,
    onViewChanged: PropTypes.func,
};
CCalendar.displayName = 'CCalendar';

exports.CCalendar = CCalendar;
//# sourceMappingURL=CCalendar.js.map
